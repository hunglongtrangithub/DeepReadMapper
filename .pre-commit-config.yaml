exclude: 'external' 

repos:
  - repo: https://github.com/pre-commit/mirrors-clang-format
    rev: v18.1.8  # Use a specific tag, NOT master/HEAD for stability
    hooks:
      - id: clang-format
        # Optional: restrict to specific file types if needed
        types_or: [c++, c, cuda]