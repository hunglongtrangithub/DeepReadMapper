name: DeepReadMapper
channels:
  - conda-forge
  - defaults
dependencies:
  # Build tools
  - cmake
  - make
  - cxx-compiler  # Provides g++/gcc
  - zig # Build

  # Core dependencies
  - openvino
  - faiss-cpu
  - zlib
  - llvm-openmp
  - cereal

  # Linting and formatting
  - pre-commit
